# T022 — Public App Store launch: hero badge (salarix-website-launch-todos)

**Status: ✅ הושלם**

## מה בוצע
- הוסר טקסט ה-"Coming soon to the App Store" מה-hero ב-`index.html` (כולל הערת ה-TODO בשורה 51).
- במקומו נוסף badge רשמי של Apple ("Download on the App Store") עם קישור לרשומת האפליקציה:
  `https://apps.apple.com/app/id6783344327` (נפתח בטאב חדש, `rel="noopener"`).
- ה-badge דו-לשוני ומתחלף אוטומטית עם מתג השפה הקיים באתר (מנגנון `data-src-en`/`data-src-he` ב-`assets/site.js` — לא נדרש שינוי ב-JS):
  - `images/badges/app-store-badge-en.svg` — "Download on the App Store"
  - `images/badges/app-store-badge-he.svg` — "להורדה ב-App Store"
  - שני הקבצים הורדו מ-API ה-badges הרשמי של Apple (toolbox.marketingtools.apple.com) ונשמרו מקומית — בלי תלות ב-CDN חיצוני. אומתו ויזואלית (רינדור ל-PNG).
- מתחת ל-badge נשארה שורת hero-note מעודכנת: "Available now on the App Store — iPhone & Mac" / "זמין עכשיו ב-App Store — לאייפון ולמק".
- CSS ב-`assets/site.css`: מחלקה חדשה `.store-badge` (גובה 52px, hover עם הרמה + drop-shadow בסגנון כפתורי ה-CTA הקיימים).

## Commits
- `9c3f1d3` — hero: replace 'Coming soon' with official App Store badge + listing link (EN/HE, local SVG assets)

## מה נשאר / הערות
- לא בוצע `git push` (בהתאם להנחיות המשימה; אין CLAUDE.md מקומי בפרויקט שמנחה אחרת).
- מחוץ לסקופ של משימה זו אך רלוונטי ל-launch: כיתובי ה-"Screenshot coming soon" בגלריית ה-screens (`index.html` שורות ~150–171) עדיין placeholders — ממתינים לצילומי המסך בפועל תחת `images/store/en|he/`.
